import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';

describe('Placeholder UI Test', () => {
    it('always passes', () => {
        expect(1).toBe(1);
    });
});
